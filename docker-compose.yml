version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: windsurf
      POSTGRES_PASSWORD: windsurf
      POSTGRES_DB: windsurf_chat
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
